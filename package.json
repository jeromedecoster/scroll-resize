{
  "name": "scroll-resize",
  "version": "0.3.0",
  "description": "Throttled window scroll and resize events",
  "repository": "jeromedecoster/scroll-resize",
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "keywords": [
    "throttle",
    "window",
    "scroll",
    "resize",
    "event"
  ],
  "author": {
    "name": "Jérôme Decoster",
    "url": "http://jeromedecoster.com"
  },
  "license": "MIT",
  "scripts": {
    "start": "budo src/index.js:bundle.js --dir src --host localhost --port 3000 --no-debug --open",
    "dev": "budo src/index.js:bundle.js --dir src --port 3000 --no-debug --live",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "budo": "^8.3.0"
  },
  "dependencies": {
    "object-funcs": "^0.2.0",
    "raf-funcs": "^0.4.0",
    "set-funcs": "^0.1.0",
    "viewport-funcs": "^0.3.0"
  }
}
